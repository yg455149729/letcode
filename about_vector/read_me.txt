这道题目是关于vector中删除重复的元素的，使用O（1）的空间复杂度。
主要是利用的是vector类的一些函数，以及注意到这些函数的小问题。
首先，关于vector的迭代器的使用，一开始使用迭代器it遍历，但其中涉及到nums.erase(it)，不知迭代器的下次在哪里，可以用it=nums.erase（it）来做到。
其次，在题目中的解法中使用的是len来修改的。使用动态修改，重复了一些多余步骤，但是还好。
另外，我们使用while而不用for循环，因为while可以实现for的功能。
