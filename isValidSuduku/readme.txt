这次是数独题，只是判断是否和规则，只需按条排序即可，但是发现C++在执行的时候，不知道为啥不会在Getchar时停止，而且在读取vector的时候会出错。注意不要
越界。





更新1：
    vector在初始化的时候会有默认值，需要先.clear()给清除掉，然后时间和空间复杂度就会提升。同时也可以解决掉上述问题。这个问题十分严重，需要注意
    
    
    
    
    
更新2：
  getchar()的时候，\n也是一个字符，因此会在buffer中当成两个字符来操作。因此会出现上述问题。须警醒。


另外采用哈希表的方法来判断是否重复。
